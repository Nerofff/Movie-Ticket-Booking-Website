@model IEnumerable<WebsiteBanVe.Models.Gio_Chieu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.gio_bat_dau)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.gio_bat_dau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_gio_chieu }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_gio_chieu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_gio_chieu })
        </td>
    </tr>
}

</table>
