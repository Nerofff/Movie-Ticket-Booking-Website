@model IEnumerable<WebsiteBanVe.Models.Loai_phim>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ten_loai_phim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ten_loai_phim)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_loai_phim }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_loai_phim }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_loai_phim })
        </td>
    </tr>
}

</table>
