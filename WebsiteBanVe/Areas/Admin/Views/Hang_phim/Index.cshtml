@model IEnumerable<WebsiteBanVe.Models.Hang_phim>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ten_hang_phim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ten_hang_phim)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_hang_phim }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_hang_phim }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_hang_phim })
        </td>
    </tr>
}

</table>
